services:

  backend:
    build: ./backend
    ports:
      - "5000:8080"

  frontend:
    build:
      context: ./frontend
      dockerfile: DockerFile
    ports:
      - "3000:80"
    depends_on:
      - backend
